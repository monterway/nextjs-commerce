'use server';

import React from 'react';
import Page from "@/components/Page";
import Form from "@/components/Form";

const Home: React.FC = () => {
    return (
        <Page>
            <Form/>
        </Page>
    );
};

export default Home;
