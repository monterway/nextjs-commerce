# NextJS Commerce

# Installation

1. Install dependencies: `npm install`
2. Start docker: `make start`
3. Run local server: `npm run dev`

# Tools

## Generator

1. Run `npm run generator`
2. Follow the guide
